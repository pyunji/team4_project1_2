<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mycompany.webapp.dao.MileageDao">
	<insert id="insertMileage" parameterType="mileage">
		insert into mileage(member_id, issue_date, amount, name, content, category, expire_date, status)
		values(#{memberId}, #{issueDate}, #{amount},#{name},#{content},#{category},#{expireDate},#{status})
	</insert>
</mapper>